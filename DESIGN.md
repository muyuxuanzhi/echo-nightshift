# 《ECHO：存档交换》双线终端悬疑 · 一页纸方案

> 方案四（复古终端 UI）+ 方案五（双主角存档跨线）的结合。
> 不做场景：整个游戏就是一台电脑的终端界面。
> 双线靠「存档 / 读档」互相传递信息。

---

## 一、核心概念（一句话）

**存档不是用来保存进度的，它是两个时间线之间唯一的通信信道。**

你在「现在」这台电脑上读到的每一份存档，都是「过去」的另一个人写给你的；你在「现在」写下的每一份存档，又会被「过去」读到。你们隔着时间，共用同一台电脑，用同一个账号，互相把真相塞进彼此的存档里。

---

## 二、双线结构（人设 + 关系）

| | A 线：现在 | B 线：过去 |
|---|---|---|
| 身份 | 深夜值夜班的你（第一人称"我"） | 上一任值夜者 / 已"消失"的那个人 |
| 时间点 | 现在，实时推进 | 七天前，按事件触发 |
| 操作对象 | 同一台终端、同一个账号 | 同一台终端、同一个账号 |
| 核心动作 | **读档**（获取过去的线索） | **写档**（给未来留线索） |

**第一人称视角统一用"我"**，不用"你"。A 线的"我"是玩家；B 线的"我"是那个人留下的文字口吻。

> 记忆点：**你以为你在读一份旧存档，其实你在跟一个死人对话。**

---

## 三、核心循环（玩家每一轮在做什么）

```
A 线（现在）
  │
  读档：翻到 B 线留下的新存档 / 修改过的系统状态
        ↓
  得到线索 → 在终端里调查（查文件、回邮件、跑命令）
        ↓
  发现"现在"缺了什么 / 打不开什么 → 写档（把指令/道具/密码写进存档）
        ↓
  切换到 B 线（过去）
        ↓
  B 线读到 A 线写的"未来提示" → 在过去的世界里执行对应操作
        ↓
  B 线写档（把结果 / 一个新线索留回给 A 线）
        ↓
  切回 A 线（现在）……
```

循环往复，直到拼出完整真相。

---

## 四、双线存档机制（本方案的核心玩法）

### 1. 通信形式（三选一或混用，建议先做前两种）

1. **文字档**：最基础。B 线写的日志、便签，A 线直接读。
2. **状态档**：B 线在过去改动了系统（比如把一个文件设成隐藏、给一个目录改了密码、打开/关闭了某个服务），这个改动会"穿越"到 A 线的现在，成为 A 线世界里的既成事实。
3. **道具档**：过去留下的某个文件、某串密钥，A 线拿到后能用来解锁现在的某个地方。

### 2. 传递规则（要写得死，玩家才能推理）

- **A 只能读、B 只能写？** 不，反过来更有味道：**两边都能写，但只有"留下存档"这个动作会跨线生效**。
- 关键规则：**A 线写下的存档，B 线是在"过去"读到的，所以 B 线只能读到 A 线在"B 线的时间点之前"才会有的信息**——时间悖论要做成谜题，而不是 Bug。
- 建议简化规则：**存档是单向的"留言板" + 双向的"系统状态"**。留言随时可见；系统状态则遵守"先写后读"的时序，玩家必须把操作按正确顺序排布，真相才成立。

### 3. 一个具体的谜题示例（让"存档玩法"立起来）

> B 线（过去）的电脑里有一份被加密的文件，密码是"事故当天门禁卡的最后四位"。
> A 线（现在）恰好能查到"事故当天的门禁记录"，知道最后四位。
> 但 A 线无法直接打开过去那份文件——因为它只存在于 B 线的时代。
> 于是 A 线把"密码"写进存档；B 线读到密码，用它在过去打开文件，
> 把文件内容**复制进一份新的存档**；A 线再读这份存档，得到内容。

→ 玩家体验到的就是：**我用自己的线索，撬开了过去的一道锁，又让过去把结果送回了现在。** 这就是"双线存档"最爽的点。

---

## 五、终端界面设计（界面即美术，不需要场景）

整款游戏就是一台复古电脑的界面，模块如下：

| 模块 | 作用 | 承载哪条线 |
|---|---|---|
| 命令行 / 终端 | 跑命令、执行系统操作 | A 线为主 |
| 文件管理器 | 翻文件夹、读文档、看元数据 | 双线 |
| 邮件客户端 | 读旧邮件、回信 | B 线为主 |
| 日志 / 笔记 | **存档本体**——写档、读档的入口 | 双线核心 |
| 系统设置 | 改时间、改权限、触发系统状态变化 | 双线 |
| 日程 / 值班表 | 锚定时间线，让"现在 vs 七天前"有参照 | 双线 |

**美术成本**：几乎为零。纯 HTML/CSS 做一个 CRT 质感的终端界面（扫描线、荧光绿/琥珀色、老旧字体），比做场景便宜一个数量级，但沉浸感和辨识度极高。

---

## 六、谜题类型（复用性高，单人可写）

1. **密码/密钥**：藏在别处的文档里，跨线传递。
2. **元数据推理**：文件的时间戳、修改人、权限变化 → 反推"过去发生了什么、谁动过"。
3. **隐藏文件**：过去被隐藏的文件，现在看不到了，需要回到过去才能看到并读走。
4. **时序谜题**：某件事必须在正确的时间点被"写进存档"，现在的世界才会改变。
5. **错位信息**：A 线以为的事，和 B 线留的档对不上 → 发现有人在说谎 / 被篡改。

---

## 七、系统拆分（单人可并行开发）

| 模块 | 内容 | 复杂度 |
|---|---|---|
| 终端 UI | 复古界面、命令行、文件/邮件/日志面板 | 中（一次性做好，复用） |
| 存档系统 | 双线状态机的读写、跨线传递 | 中（核心，先做） |
| 对话/文本 | 文档、邮件、日志的展示与检索 | 低 |
| 谜题逻辑 | 密码校验、状态解锁、条件判断 | 低（量多但简单） |
| 时间线管理 | A/B 线切换、事件触发点 | 中 |
| 存档持久化 | 本地保存玩家进度（真存档，区别于剧情"存档"） | 低 |

---

## 八、剧本框架（可控、不烂尾）

- **结构**：一个封闭地点（值班室 / 研究所 / 病房），一个核心案件，一段七天时间差。
- **B 线内容**：那个人的值班日志、未发出的邮件、被删到一半的文件——碎片化，每份都是一块拼图。
- **A 线内容**：现在正在发生的异常（系统报错、陌生进程、门禁告警），逼玩家去翻过去。
- **真相**：藏在"现在和过去对不上"的那几处细节里，最后在终端里拼出"那个人到底怎么了"。

---

## 九、单人开发排期（最简可玩版优先）

| 阶段 | 内容 | 目标 |
|---|---|---|
| Day 1–2 | 终端 UI + 双线切换骨架 | 能看、能切换、能写档读档 |
| Day 3–4 | 存档系统（状态机 + 跨线传递）+ 1 个完整谜题 | 打通"读→调查→写→切线→读"全循环 |
| Day 5–6 | 补 3~5 个谜题 + 邮件/日志内容 | 一段可玩的 20 分钟小故事 |
| Day 7 | 收尾结局 + 一键部署到 GitHub Pages | 可试玩、可录屏 |

> 关键取舍：**先保"存档跨线"这一个核心机制，跑通一个谜题就够；弹幕/动画/复杂演出全部砍掉**（终端界面本来就不需要这些）。

---

## 十、最小可玩版（MVP）范围

- 一台终端，5 个模块（命令行、文件、邮件、日志、设置）。
- 双线：A（现在）+ B（七天前），可随时切换。
- 1 个核心案件，3 个跨线谜题，1 个结局。
- 全程第一人称"我"，无场景、无角色立绘、无动画。
- 一键网页试玩。

---

## 十一、记忆点收束（面试/简历一句话）

> **"一台电脑、两个时间、一个共享的存档账号——你们隔着七天，用存档交换真相。"**
